__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/
*.egg-info/
.DS_Store

# Any project's personal data files shouldn't be committed
data/*.json
!data/.gitkeep

# Personal credentials / league config — never commit real ones
config.json

# Downloaded NFL stats cache — large, regenerable, not source
.cache/
